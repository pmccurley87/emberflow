# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
studio-dist/
*.local

# Runner secrets (dev tool)
emberflow.secrets.json
emberflow.environments.json
# Pre-migration backup kept by the secrets-hardening migration; holds the
# original inline secret values, so it must never be committed.
emberflow.environments.json.bak

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.tgz
dist-pack/
.playwright-mcp/
